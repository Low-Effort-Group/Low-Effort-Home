[package]
name = "media"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
image = { version = "0.25", features = ["jpeg", "png", "avif", "gif", "ico", "webp"] }
wasm-bindgen = "0.2.114"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"